# Loaded by `next build` so the client bundle inlines the CDN origin for `cloudflare-image-loader.ts`.
# Not a secret (public object hostname). Override with CI/build env if you use a different CDN.
NEXT_PUBLIC_R2_PUBLIC_BASE_URL=https://cdn.loyaltering.online
