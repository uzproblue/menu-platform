/** A4 page size at 96 DPI (screen editing). */
export const A4_WIDTH_PX = 794;
export const A4_HEIGHT_PX = 1123;

/** Multiplier for `stage.toDataURL` (~288 DPI effective from 96 DPI base). */
export const A4_EXPORT_PIXEL_RATIO = 3;
